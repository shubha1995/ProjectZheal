import {View, Text, } from 'react-native';
import React from 'react';
import { styles } from '../common/styles';

const Title = () => {
  return (
    <View>
      <Text style={styles.title}>Choose your device to setup</Text>
    </View>
  );
};



export default Title;
